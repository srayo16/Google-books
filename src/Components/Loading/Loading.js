import React from 'react';

const Loading = () => {
    return (
        <div className='container mx-auto text-center my-40'>
            <button className="btn loading">loading</button>
        </div>
    );
};

export default Loading;